import colors from "./colors";

const { primary } = colors;

const borders = {
	borderColor: primary.main,

	borderWidth: {
		closeBtn: 5,
		input:4,
		probe: 6,
	},

	borderRadius: {
		main: 16,
		probe:120,
		card: 8,
		closeButton: 44,
		input: 8,
		badge: 4,
	},
};

export default borders;
